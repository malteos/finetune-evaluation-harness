name: Publish distributions 📦 to PyPI

on:
  push:
    branches: [main]

jobs:
  build-n-publish:
    name: Build and publish 🐍 distributions 📦 to PyPI
    runs-on: ubuntu-latest
    #working-directory: finetune-evaluation-harness
    steps: 
    - uses: actions/checkout@v3
    - name: Setup
      uses: actions/setup-python@v4
      with:
        python-version: 3.8
    - name: Build distribution
      run: |
        python setup.py sdist
    - name: Publish distribution
      working-directory: finetune-evaluation-harness
      if: startsWith(github.ref, 'refs/tags')
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        #password: ${{ secrets.PYPI_API_TOKEN }}
        password: 'pypi-AgEIcHlwaS5vcmcCJGRhMDYxNjU4LTMxZjktNGQyNi05ZGU3LTc3NDY4MTc4ZDc4ZAACKlszLCJkN2ViNzIzMC1mNmI1LTRhNDEtYmFjMC05MTg5YjI2NzNmOTYiXQAABiAxt7CUMACgY12aiHkZmQRme3OVXQ5zcicA18Ula_WsNQ'
